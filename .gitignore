# Build
build/
build-*/
cmake-build-*/
out/
*.o
*.a
*.lib
*.obj

# Node / Vite
node_modules/
ui/dist/
.vite/

# GENERATED: synced from the JUCE checkout by ui/scripts/sync-juce-frontend.mjs
ui/src/juce/

# Screenshots and recordings from tools/screenshot_ui.mjs and
# tools/record_ui_motion.mjs. Regenerable from the built bundle, and large -
# the point of them is to be LOOKED at, not to be kept.
.screenshots/
*.webm

# IDE / OS
.vscode/
.idea/
*.xcodeproj/
*.xcworkspace/
.DS_Store
Thumbs.db

# Secrets — the licensing private key NEVER enters the repo
*.pem
*.p12
*.key
.env
.env.local

# Cloudflare Workers local state (a local D1 lives in here).
.wrangler/
backend/worker-configuration.d.ts   # generated by `wrangler types`, not needed to build
